# Kbuild — read by the kernel build system when invoked via:
#   make -C /lib/modules/$(uname -r)/build M=$(PWD) modules
#
# Only kernel build rules belong here; userspace targets live in Makefile.

obj-m := orico_cf1000_leds.o
